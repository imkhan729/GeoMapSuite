'use client';
import {useEffect,useState} from 'react';
const ZONES=[['Eastern','UTC−5 / UTC−4','New York, Atlanta'],['Central','UTC−6 / UTC−5','Chicago, Dallas'],['Mountain','UTC−7 / UTC−6','Denver, Phoenix*'],['Pacific','UTC−8 / UTC−7','Los Angeles, Seattle'],['Alaska','UTC−9 / UTC−8','Anchorage'],['Hawaii-Aleutian','UTC−10','Honolulu']];
export function UsTimeZoneMapView(){const [zone,setZone]=useState(0);const [now,setNow]=useState(new Date());useEffect(()=>{const id=window.setInterval(()=>setNow(new Date()),1000);return()=>window.clearInterval(id)},[]);return <div className="space-y-5"><div className="grid gap-3 sm:grid-cols-3"><Fact label="Zones shown" value="6" detail="US time-zone groups"/><Fact label="Clock" value={now.toLocaleTimeString()} detail="Your browser's local clock"/><Fact label="Reference" value="UTC offsets" detail="Standard / daylight notation"/></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex flex-wrap gap-2">{ZONES.map((z,i)=><button key={z[0]} type="button" onClick={()=>setZone(i)} className={`rounded-full border px-3 py-2 text-sm font-bold ${zone===i?'border-blue-700 bg-blue-700 text-white':'border-slate-300 hover:bg-blue-50'}`}>{z[0]}</button>)}</div><div className="grid gap-4 lg:grid-cols-[1fr_1.5fr]"><div className="rounded-xl bg-blue-50 p-5"><p className="text-xs font-bold uppercase tracking-wide text-blue-900">Selected US time zone</p><h2 className="mt-1 text-2xl font-black">{ZONES[zone][0]}</h2><p className="mt-2 text-sm text-slate-700">{ZONES[zone][1]}</p><p className="mt-1 text-sm text-slate-600">Example places: {ZONES[zone][2]}</p></div><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{ZONES.map((z,i)=><button key={z[0]} type="button" onClick={()=>setZone(i)} className={`min-h-24 rounded-xl border p-3 text-left ${zone===i?'border-blue-500 ring-2 ring-blue-100':'border-slate-200'}`}><span className="block h-3 rounded bg-gradient-to-r from-blue-200 to-blue-700"/><strong className="mt-2 block text-sm">{z[0]}</strong><span className="text-xs text-slate-600">{z[1]}</span></button>)}</div></div><p className="mt-4 text-xs leading-5 text-slate-500">*Arizona generally observes Mountain Standard Time year-round. Actual boundaries and daylight-saving rules can vary by state, county, and tribal jurisdiction; verify official local time for critical schedules.</p></div></div>}
function Fact({label,value,detail}:{label:string,value:string,detail:string}){return <div className="rounded-xl border border-blue-100 bg-white p-4 shadow-sm"><div className="text-[10px] font-bold uppercase tracking-wide text-slate-500">{label}</div><div className="mt-1 text-xl font-black">{value}</div><div className="mt-1 text-xs text-slate-600">{detail}</div></div>}
