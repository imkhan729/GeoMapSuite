'use client';
import {useEffect,useState} from 'react';
const ZONES=[['UTC−12 to −9','Baker Island · Alaska region'],['UTC−8 to −5','Pacific · Eastern Americas'],['UTC−4 to −1','Atlantic · South America'],['UTC±0','Greenwich · West Africa'],['UTC+1 to +3','Europe · East Africa'],['UTC+4 to +8','Gulf · India · China'],['UTC+9 to +12','Japan · Australia · Pacific'],['UTC+13 to +14','New Zealand · Pacific islands']];
export function WorldTimeZoneMapView(){const [zone,setZone]=useState(3);const [now,setNow]=useState(new Date());useEffect(()=>{const id=window.setInterval(()=>setNow(new Date()),1000);return()=>window.clearInterval(id)},[]);return <div className="space-y-5"><div className="grid gap-3 sm:grid-cols-3"><Fact label="Offset range" value="UTC−12 to +14" detail="Global civil-time range"/><Fact label="Current browser clock" value={now.toLocaleTimeString()} detail="Updates every second"/><Fact label="Date boundary" value="Near 180°" detail="International Date Line"/></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="mb-4 text-sm leading-6 text-slate-600">Select a UTC band to explore representative regions. Time-zone borders are political and can differ from straight longitude bands.</p><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{ZONES.map((z,i)=><button key={z[0]} type="button" onClick={()=>setZone(i)} className={`min-h-28 rounded-xl border p-4 text-left ${zone===i?'border-emerald-600 bg-emerald-50 ring-2 ring-emerald-100':'border-slate-200 hover:border-emerald-300'}`}><span className="block h-3 rounded bg-gradient-to-r from-emerald-200 to-emerald-700"/><strong className="mt-3 block text-sm">{z[0]}</strong><span className="mt-1 block text-xs text-slate-600">{z[1]}</span></button>)}</div><div className="mt-5 rounded-xl bg-emerald-50 p-5" aria-live="polite"><p className="text-xs font-bold uppercase tracking-wide text-emerald-900">Selected UTC band</p><h2 className="mt-1 text-2xl font-black">{ZONES[zone][0]}</h2><p className="mt-1 text-sm text-slate-700">Representative regions: {ZONES[zone][1]}</p></div><p className="mt-4 text-xs leading-5 text-slate-500">This is a global orientation guide, not a live database of every IANA zone. For travel, scheduling, and software, use the named IANA time zone and the local authority's current rules.</p></div></div>}
function Fact({label,value,detail}:{label:string,value:string,detail:string}){return <div className="rounded-xl border border-emerald-100 bg-white p-4 shadow-sm"><div className="text-[10px] font-bold uppercase tracking-wide text-slate-500">{label}</div><div className="mt-1 text-xl font-black">{value}</div><div className="mt-1 text-xs text-slate-600">{detail}</div></div>}
