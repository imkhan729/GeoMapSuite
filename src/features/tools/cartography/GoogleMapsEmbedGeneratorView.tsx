'use client';
import {useMemo,useState} from 'react';
export function GoogleMapsEmbedGeneratorView(){const [query,setQuery]=useState('Eiffel Tower, Paris');const [zoom,setZoom]=useState(14);const [width,setWidth]=useState('100%');const [height,setHeight]=useState('450');const [copied,setCopied]=useState(false);const src=useMemo(()=>`https://www.google.com/maps?q=${encodeURIComponent(query)}&z=${zoom}&output=embed`,[query,zoom]);const code=`<iframe src="${src}" width="${width}" height="${height}" style="border:0" allowfullscreen loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>`;const copy=async()=>{try{await navigator.clipboard.writeText(code);setCopied(true);window.setTimeout(()=>setCopied(false),1800)}catch{setCopied(false)}};return <div className="grid gap-5 lg:grid-cols-[300px_1fr]"><div className="space-y-4 rounded-2xl border border-indigo-200 bg-white p-5 shadow-sm"><label htmlFor="embed-place" className="text-sm font-bold text-slate-800">Place, address, or landmark</label><input id="embed-place" value={query} onChange={e=>setQuery(e.target.value)} className="w-full rounded-lg border border-slate-300 px-3 py-2 text-sm"/><label htmlFor="embed-zoom" className="text-sm font-bold text-slate-800">Zoom: {zoom}</label><input id="embed-zoom" type="range" min="1" max="20" value={zoom} onChange={e=>setZoom(Number(e.target.value))} className="w-full accent-indigo-700"/><div className="grid grid-cols-2 gap-2"><label className="text-xs font-bold">Width<input value={width} onChange={e=>setWidth(e.target.value)} className="mt-1 w-full rounded border px-2 py-1"/></label><label className="text-xs font-bold">Height<input value={height} onChange={e=>setHeight(e.target.value)} className="mt-1 w-full rounded border px-2 py-1"/></label></div><button type="button" onClick={copy} className="w-full rounded-lg bg-indigo-800 px-4 py-2.5 text-sm font-bold text-white hover:bg-indigo-900">{copied?'Code copied':'Copy embed code'}</button><p className="text-xs leading-5 text-slate-500">This client-side generator creates a standard Google Maps embed URL. It does not geocode, store your query, or require an API key.</p></div><div className="space-y-4"><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><iframe title={`Google Maps preview for ${query}`} src={src} width="100%" height="360" style={{border:0}} loading="lazy" referrerPolicy="no-referrer-when-downgrade"/></div><div><label htmlFor="embed-code" className="text-sm font-bold text-slate-800">HTML embed code</label><textarea id="embed-code" readOnly value={code} rows={6} className="mt-2 w-full rounded-xl border border-slate-300 bg-slate-50 p-3 font-mono text-xs"/></div></div></div>}
